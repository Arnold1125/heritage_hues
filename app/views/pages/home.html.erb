<div class="banner-main" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= image_path "banner.jpg" %>);">
  <div class="container d-flex flex-column justify-content-center align-items-center">
    <h1>Color your life with heritage</h1>
  </div>
</div>

<div class="container">
  <h1 class="mt-4">All clothings</h1>
  <div class="cards">
    <% @clothings.each do |clothing| %>
      <%= link_to clothing_path(clothing), class: "card-trip-link" do %>
        <% if clothing.photo.attached? %>
          <div class="card-trip">
              <%= cl_image_tag clothing.photo.key, alt: "Image of #{clothing.name}" %>          <div class="card-trip-infos">
              <div>
                <h2><%= clothing.name %></h2>
                <p><%= clothing.description %></p>
                <p><%= clothing.country %></p>
              </div>
              <h2 class="card-trip-pricing">¥<%= clothing.price %></h2>
            </div>
          </div>
        <% end %>
      <% end %>
      </%= link_to %>
    <% end %>
  </div>
</div>
